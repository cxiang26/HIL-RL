defaults:
  - _self_
  - robot_type@_global_: franka
  - task@_global_: close_trashbin_franka_1028
  - override hydra/launcher: submitit_local



fake_env: false
control_mode: pose
lerobot_config_path: None
freeze_actor: false
use_human_intervention: false
policy_type: silri
ego_mode: false
use_force: false
load_classifier: true
use_left_arm_reset: false  # 是否用左臂插值做重置；在 task yaml 中可覆盖（如 a2d_left_right）
# Actor 加载本地策略时可通过命令行覆盖，例如：pretrained_policy_name_or_path=experiments/a2d_left_right/expert_model_silri
pretrained_policy_name_or_path: null



hydra:
  run:
    dir: ./exp_local/${now:%Y.%m.%d}/${hydra.job.override_dirname}
  sweep:
    dir: ./exp_local
    subdir: ${now:%Y.%m.%d}/${hydra.job.override_dirname}
